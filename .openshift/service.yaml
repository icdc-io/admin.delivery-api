apiVersion: v1
kind: Service
metadata:
  labels:
    app: services-api
  name: services-api
spec:
  ports:
    - name: 3000-tcp
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    deploymentconfig: services-api
  sessionAffinity: None
  type: ClusterIP
